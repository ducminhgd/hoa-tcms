meta {
  name: Update User
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/users/{{userId}}
  body: json
  auth: inherit
}

body:json {
  {
    "fullname": "John Updated",
    "status": "ACTIVE"
  }
}

docs {
  # Update User

  Partial update of a user. Requires `user:update` permission.

  - `200 OK`
  - `404 Not Found`
  - `422 Unprocessable Entity` — validation error
}
