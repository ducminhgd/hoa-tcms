meta {
  name: Delete Share
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/share/1
  body: none
  auth: inherit
}

docs {
  # Delete Share

  Removes a sharing record (hard delete). Requires `share:delete` permission.

  - `204 No Content` — removed
  - `403 Forbidden` — missing permission
}
