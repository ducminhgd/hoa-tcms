meta {
  name: List Shares
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/share/test_case/1
  body: none
  auth: inherit
}

docs {
  # List Shares

  Returns all sharing records for a specific resource. Requires `share:read` permission.

  - `200 OK` — array of sharing records with resolved usernames
  - `403 Forbidden` — missing permission
}
