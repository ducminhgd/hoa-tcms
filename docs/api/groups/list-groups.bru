meta {
  name: List Groups
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/groups?page=1&limit=25
  body: none
  auth: inherit
}

docs {
  # List Groups

  Returns a paginated list of groups.

  Query params: `page`, `limit` (max 100), `search`, `status`

  - `200 OK`
}
