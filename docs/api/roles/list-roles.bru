meta {
  name: List Roles
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/roles?page=1&limit=25
  body: none
  auth: inherit
}

docs {
  # List Roles

  Returns a paginated list of roles.

  Query params: `page`, `limit` (max 100)

  - `200 OK`
}
