meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: inherit
}

docs {
  # Logout

  Destroys the current session.

  - `204 No Content` — logged out, session cookie cleared
}
